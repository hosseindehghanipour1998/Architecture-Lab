


// Instantiate the module
Data_Memory instance_name (
    .clk(clk), 
    .ADDRESS(ADDRESS), 
    .WRITE_DATA(WRITE_DATA), 
    .MEM_WRITE(MEM_WRITE), 
    .MEM_READ(MEM_READ), 
    .READ_DATA(READ_DATA)
    );


