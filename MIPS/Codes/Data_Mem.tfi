


// Instantiate the module
Data_Mem instance_name (
    .clk(clk), 
    .memRead(memRead), 
    .memWrite(memWrite), 
    .address(address), 
    .write_data(write_data), 
    .read_data(read_data)
    );


